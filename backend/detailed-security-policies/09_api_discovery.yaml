policy_name: API Discovery & Documentation
definition: >
  All APIs must be registered, documented, and versioned.
explanation: >
  Prevents shadow APIs and untracked risks.
kong_solution:
  - decK sync with OpenAPI spec
  - Developer portal
references:
  - OWASP: API9
  - ISO 27001: A.8
sample_kong_config:
  name: custom-plugin
  config:
    example_field: example_value
    enabled: true
